# include <stdio.h>

int main(){
    // A declaração e a carga de um vetor podem ser realizadas sem indicar qual o número de elementos no vetor
    // Nesse caso, o compilador vai criar um vetor cin antos elements quantas as cargas inicias.
    
    char letras[] = {'c','a','l','c'};
    
}